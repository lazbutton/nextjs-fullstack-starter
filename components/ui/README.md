# UI Components

This directory contains shadcn/ui components. Add new components using:

```bash
npx shadcn@latest add [component-name]
```

Available components: https://ui.shadcn.com/docs/components

